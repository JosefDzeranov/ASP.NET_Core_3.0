@using OnlineShop.Db.Models

@model CartViewModel

<h2 align="center">Корзина</h2>
@if (Model == null)
{
    <div class="404">
        <h3 align="center">Корзина пустая</h3>
        <img class="card-img-top" src="/images/404.jpg" />
    </div>
}
else
{
    <div class="row">
        <div class="col">
            <table class="table table-striped">
                <thead class="table-light" align="center">
                    <tr>
                        <th scope="col">Фото</th>
                        <th scope="col">Название</th>
                        <th scope="col">Цена (руб.)</th>
                        <th scope="col">Стоимость (руб.)</th>
                        <th scope="col">Количество</th>
                        <th scope="col">Изменить</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var cartItem  in Model.Items)
                    {
                        <tr>
                            <td><img src=@cartItem.Product.ImagePath style="width: 80px; height: 80px;" /></td>
                            <td><a href="/product/index/@cartItem.Product.Id">@cartItem.Product.Name</a></td>
                            <td>@cartItem.Product.Cost</td>
                            <td>@cartItem.Cost</td>
                            <td>@cartItem.Count</td>
                            <td><a href="/cart/RemoveCountProductCart/?cartItem.productId=@cartItem.Product.Id&cartid=@cartItem.ItemId" type="button" class="btn btn-danger">-</a>
                                <a href="/cart/add/?cartItem.productId=@cartItem.Product.Id" type="button" class="btn btn-danger">+</a></td>
                        </tr>
                    }
                </tbody>
                <tr class="text-right">
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td><h3>Итого</h3></td>
                    <td><h3>@Model.Cost</h3></td>
                </tr>
            </table>
            <div class="text-right">
                <a type="button" class="btn btn-outline-danger" href="/cart/RemoveCartUser/?userId=@Model.UserId" role="button">Очистить корзину</a>
            </div>
            <div class="text-center">
                <a class="btn btn-dark" href="/home/index" role="button">Продолжить покупки</a>
                <a class="btn btn-dark" href="/order/OrderMaking" role="button">Оформить заказ</a>
            </div>
        </div>
    </div>

}