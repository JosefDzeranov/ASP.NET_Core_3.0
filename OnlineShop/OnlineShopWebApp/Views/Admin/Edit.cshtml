@model Product

<form method="post">
    <div class="row">
        @await Html.PartialAsync("AdminLeftPanel")
        <div class="col text-center">
            <h3>Редактировать информацию о продукте</h3>
            <input type="hidden" name="id" value="@Model.Id"/>
            <label>Название</label>
            <input type="text" name="name" value="@Model.Name" />
            <br />
            <label>Цена</label>
            <input type="text" name="cost" value="@Model.Price" />
            <br />
            <label>Описание</label>
            <input type="text" name="description" value="@Model.Description" />
            <br />
            <label>Количество страниц</label>
            <input type="text" name="pages" value="@Model.ImagesPath" />
            <br />
            <button class="btn btn-dark" type="submit" asp-controller="Admin" asp-action="Edit" asp-route-productId="@Model.Id">Сохранить</button>
        </div>
    </div>
</form>